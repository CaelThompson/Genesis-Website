---
import SearchLayout from "../layouts/SearchLayout.astro";
import Search from "../components/landing/SearchEngine/SearchListPage.astro"
import '../styles/global.css'
---

<SearchLayout>

<main class="grid min-h-full place-items-center bg-white px-6 py-24 sm:py-32 lg:px-8 custom-main">
  <div class="text-center">
    <p class="text-base font-semibold text-green-600">404</p>
    <h1 class="mt-4 text-3xl font-bold tracking-tight sm:text-5xl custom-heading">Out of Bounds</h1>
    <p class="mt-6 text-base leading-7 text-gray-600">
      You've stumbled upon an uncharted feature. This could be a relic of the past or an unfinished page.
    </p>
    <p class="mt-2 text-base leading-7 text-gray-600">
      Please return to the search engine and continue exploring.
    </p>
    <div class="mt-10 flex items-center justify-center gap-x-6">
      <a href="/" class="rounded-md px-3.5 py-2.5 text-sm font-semibold text-white shadow-sm custom-main-btn">
        Go back home
      </a>
      <a href="/searchresult" class="text-sm rounded-md font-semibold text-gray-900 hover:ring-1 ring-zinc-300 px-3.5 py-2.5 custom-link">
        Return to search <span aria-hidden="true">&rarr;</span>
      </a>
    </div>
  </div>
</main>

<!-- Move custom styles here to ensure they apply last -->
<style>
  .custom-main {
    background: linear-gradient(to bottom, #f9fafb, #e5e7eb) !important;
  }

  .custom-heading {
    color: #1f2937 !important;
    text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.1) !important;
  }

  .custom-main-btn {
    background-color: #10b981 !important; /* Tailwind's green-500 */
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .custom-main-btn:hover {
    background-color: #059669 !important; /* Tailwind's green-600 */
    transform: scale(1.05);
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  }

  .custom-link {
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .custom-link:hover {
    transform: scale(1.05);
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.08);
  }
</style>

</SearchLayout>